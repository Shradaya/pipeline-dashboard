CREATE TABLE IF NOT EXISTS raw.raw_data (
    id VARCHAR(255),
    user_id VARCHAR(255),
    emp_id VARCHAR(255),
    team_manager_id VARCHAR(255),
    designation_id VARCHAR(255),
    designation_name VARCHAR(255),
    first_name VARCHAR(255),
    middle_name VARCHAR(255),
    last_name VARCHAR(255),
    email VARCHAR(255),
    is_hr VARCHAR(255),
    is_supervisor VARCHAR(255),
    allocations TEXT,
    leave_issuer_id VARCHAR(255),
    current_leave_issuer_id VARCHAR(255),
    leave_issuer_first_name VARCHAR(255),
    leave_issuer_last_name VARCHAR(255),
    current_leave_issuer_email VARCHAR(255),
    department_description VARCHAR(255),
    start_date VARCHAR(255),
    end_date VARCHAR(255),
    leave_days VARCHAR(255),
    reason TEXT,
    status VARCHAR(255),
    remarks TEXT,
    leave_type_id VARCHAR(255),
    leave_type_name VARCHAR(255),
    default_days VARCHAR(255),
    transferable_days VARCHAR(255),
    is_consecutive VARCHAR(255),
    fiscal_id VARCHAR(255),
    fiscal_start_date VARCHAR(255),
    fiscal_end_date VARCHAR(255),
    fiscal_is_current VARCHAR(255),
    created_at VARCHAR(255),
    updated_at VARCHAR(255),
    is_converted VARCHAR(255),
    CONSTRAINT raw_api_data_uk UNIQUE (id)
);
